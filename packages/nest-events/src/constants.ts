/**
 * @ccatto/nest-events - Injection Tokens
 */

/** Injection token for the events config (e.g. the Prisma model name). */
export const CATTO_EVENTS_CONFIG = 'CATTO_EVENTS_CONFIG';

/** Injection token that resolves to the app's Prisma service/client. */
export const CATTO_EVENTS_PRISMA = 'CATTO_EVENTS_PRISMA';
